/** Sandbox tokens for HTML preview iframes: opaque origin, no same-origin,
 *  no top navigation (mirrors DSH-better-sidebar's boundary). */
export const HTML_IFRAME_SANDBOX = 'allow-scripts allow-popups allow-downloads allow-modals'
